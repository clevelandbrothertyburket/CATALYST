import QrPlatform from '@/components/QrPlatform';

export const metadata = {
  title: 'Catalyst — Link Platform',
  description: 'Cleveland Brothers short links, QR codes, and click analytics.',
};

export default function QrGenerationPage() {
  return <QrPlatform />;
}
